# AI Configuration
# Enable local T5 model (requires ~1GB download, consumes RAM)
USE_T5_MODEL=false

# Enable Google Gemini (Cloud AI - Recommended for lightweight)
# Get key from: https://aistudio.google.com/app/apikey
GEMINI_API_KEY=

# Database (Optional)
DATABASE_URL=sqlite:///./studykit.db
